[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tflite-runtime"
version = "2.18.0"
description = "Compatibility stub for platforms that use openWakeWord with onnxruntime fallback."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "onnxruntime>=1.10.0,<2",
]

[tool.setuptools]
py-modules = []